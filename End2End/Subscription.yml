info:
  name: Subscription
  type: http
  seq: 5

http:
  method: get
  url: "{{Stage_VOD}}/api/v1/panel/subscription/?page=1"
  headers:
    - name: accept
      value: application/json
    - name: Authorization
      value: Bearer {{access_token}}
    - name: X-CSRFTOKEN
      value: iLva68eDifnRLajeoD57UgWD0zfnGQDzM6RLFjURVvB2TM7MwaauOI63h7pW0qEx
  params:
    - name: page
      value: "1"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const responseTime = res.getResponseTime();



        const jsonData = res.getBody();

        test("The user subscription shown correctly", () => {
          expect(res.status).to.equal(200);
        });

        test("Response time is less than 500ms", () => {
          expect(res.responseTime).to.be.below(500);
        });

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
